/**
 *  Vectores
 **/

let array = [1, 2, 3];
array[4]; //6

// Insert to tail
array.push(4); // [1, 2, 3, 4]

// Insert/update values anywhere
array[8] = 9; // [1, 2, 3, 4, null, 9]

// Deleting last element from the array
array.pop(); // [1, 2, 3, 4, null]
